# 사용자 화면 문구 감사 (claim audit)

- 기준 브랜치: `feat/evaluation-baseline` (시작 커밋 `b3c1099`)
- 검색어: 정확도, 신뢰도, 검증 완료, 검증된, 오류 없음, 통관 가능, 제출 가능, 자동 신고, 공식 판정, 관세청 사례, 품목분류 사례, 실시간, 보장, 완벽, 100%, 12,469, 일치 가능성
- 범위: `src/`, `docs/`, `public/` (테스트 파일·주석·CSS 너비 값·입력 예시 placeholder 제외, 상업송장 템플릿 제외)
- 원칙: **이번 작업에서 자동 수정한 것은 HSK 고정 퍼센트 표시와 출처 UUID 노출 두 가지뿐**이다. 나머지는 근거를 확인한 뒤 팀이 결정한다.

## 이번에 수정한 항목

| 위치 | 수정 전 | 수정 후 | 이유 |
|---|---|---|---|
| `src/components/import/ImportTradeFlow.tsx` (G. HSK 자동추천) | `추천 신뢰도 85%` / `65%` / `55%` | `추천 1순위` · `추천 2순위` … + 정렬 기준 안내문 | 퍼센트는 코드에 고정된 값(0.85/0.65/0.55)이라 측정된 정확도가 아님. 후보 순서·저장값(`confidence`)은 그대로 |
| `src/components/import/ImportAnalysisSummary.tsx` (D. 품목정보) | `추출 출처: 3f2a…-uuid, …` | `추출 출처: C/I · invoice.pdf, P/L · packing.pdf` (못 찾으면 `출처 문서 확인 불가`) | 내부 식별값(UUID) 노출. 페이지 번호·원문 발췌는 응답에 없어 표시하지 않음 |

## 자동 수정하지 않은 항목

| # | 파일:줄 | 현재 문구 | 위험 | 근거 확인 결과 | 권장 조치 | 우선순위 |
|---|---|---|---|---|---|---|
| 1 | `src/components/ShipperWorkspaceForm.tsx:792` | 관세청 공식 HS 품목분류 사전 12,469건과 대조해 검증한 추천입니다. | "검증한"이 정답 검증으로 읽힘 | 공식 사전에 존재하는 코드인지 대조한 것(코드 실재성)이지 품목분류가 맞는지 검증한 것이 아님 | "관세청 공식 HS 사전(12,469건)에 있는 코드 중에서 고른 추천입니다." 식으로 범위를 명확히 | 높음 |
| 2 | `src/components/ShipperWorkspaceForm.tsx:802` | 높은 일치 가능성 / 추가 확인 필요 | 확률·정확도처럼 읽힘 | `confidenceLabel`은 AI가 준 라벨(높음/보통) 파싱값. 측정된 적중률 아님 | "추천 1순위" 식 순위 표기 또는 "근거 충분 / 정보 보완 필요" | 높음 |
| 3 | `src/services/importRiskService.ts:407` | HS Code 신뢰도 낮음 — AI 추천 후보의 최고 신뢰도가 70% 미만입니다. | 화면에서 퍼센트는 없앴는데 카드에 "신뢰도 70%"가 남음 | 기준값은 고정 confidence(0.85/0.65/0.55). 폴백 후보만 있을 때 뜸 | "추천 근거가 부족한 품목입니다" 식으로 문구만 교체(판정 조건은 유지) | 중간 |
| 4 | `src/components/GuidePanel.tsx:145` | 관세청 품목분류 사례와 대조해 후보를 추천해요. | 사례(결정례) DB를 쓰는 것으로 오해 | 실제로는 HSK 공식 코드·품명 사전 검색 + AI 정렬. 품목분류 결정사례 데이터는 사용하지 않음 | "관세청 공식 HS 코드 사전에서 후보를 찾아 추천해요." | 높음 |
| 5 | `src/components/import/ImportTradeFlow.tsx:741` | HSK 코드 확정값 검증 완료 (진행 로그) | "검증 완료"가 정답 확인으로 읽힘 | 사용자가 고른 10자리 코드의 형식·사전 존재 여부 확인 단계 | "확정값 형식 확인 완료" | 낮음 |
| 6 | `src/components/import/ImportTradeFlow.tsx:1268` | 검증된 10자리 코드를 직접 입력해야 합니다. | 낮음(사용자에게 확인 책임을 안내하는 문장) | 안내 목적 | 유지 가능, 필요 시 "확인된" | 낮음 |
| 7 | `src/App.tsx:3046, 3074` | 완벽 테스트 / 완벽 테스트 모드 | DEV 전용 버튼(`IS_DEV_TEST_ENABLED`) | 운영 화면에는 노출되지 않음 | 시연 녹화에 잡히지 않게만 주의 | 낮음 |
| 8 | `src/App.tsx:4222` | 추출값 기반 대조라 100% 정확하지 않을 수 있습니다. | 없음(한계 고지 문구) | — | 유지 | — |

## 해당 없음으로 확인한 검색어

"오류 없음", "통관 가능", "제출 가능", "자동 신고", "공식 판정", "보장"은 사용자 화면 문구에서 발견되지 않았다. "실시간"은 주석(알림 구독·재평가 설명)에만 있다.

## 보고서·발표에 쓸 때 주의

- 평가 스크립트 예제(`evaluation/examples/`)의 수치는 **합성 예제로 스크립트 동작을 확인한 값**이며 PortAI의 성능 수치가 아니다.
- 실제 측정 전에는 "정확도 N%" 대신 "파일럿 평가 설계 완료, 측정 예정"으로 쓴다.
